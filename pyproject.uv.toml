[project]
name = "helperScripts"
version = "0.1.0"
description = "General-purpose Python utilities for the matrix project."
readme = "README.md"
requires-python = ">=3.11,<4.0"
license = { text = "MIT" }
project.authors = [
  { name = "Suhail", email = "suhailece@gmail.com" },
]
dependencies = [
  "jinja2 ^3.1.4",
  "keyring ^25.5.0",
  "python-dotenv ^1.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# Original [tool.poetry] has been migrated to PEP 621 by migrate2uv.sh
